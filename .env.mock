# 목 데이터 미리보기 빌드 전용. (`npm run build:mock`)
#
# 백엔드 연동 전에 화면만 확인하려고 만든 모드다.
# 실제 서비스 빌드(`npm run build`)에는 영향이 없다 — Vite 는 `--mode mock` 일 때만
# 이 파일을 읽는다. 그래서 목을 켜는 설정이 실배포로 새어 나갈 일이 없다.
VITE_USE_MSW=true
